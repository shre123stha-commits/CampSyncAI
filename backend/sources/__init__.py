"""Data source adapters.

Each adapter normalises an external platform into the shared `Task` and
`Lecture` models, so the planner never needs to know where data came from.

Planned: UploadSource, ICSSource, ClassroomSource (OAuth), MoodleSource.
"""
