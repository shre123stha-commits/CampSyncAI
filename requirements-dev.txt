# Everything needed to run the whole app on one machine: backend, website
# and tests. Deployment does not use this file - Render builds the backend
# and frontend separately from their own Dockerfiles.
#
#   py -3.12 -m venv .venv
#   .\.venv\Scripts\Activate.ps1        (Windows)
#   source .venv/bin/activate           (macOS / Linux)
#   pip install -r requirements-dev.txt

# --- Backend ---
fastapi>=0.141.1
uvicorn>=0.52.0
pydantic>=2.13.4
python-dotenv>=1.2.2
python-docx>=1.2.0
sqlmodel>=0.0.22
bcrypt>=4.2.0
python-multipart>=0.0.9
icalendar>=6.0.0
cryptography>=43.0.0
requests>=2.32.0
truststore>=0.10.0

# AI. langchain-ollama is required even when using a hosted provider,
# because the module is imported unconditionally at startup.
langchain>=1.3.14
langchain-ollama>=1.1.0
langchain-openai>=0.2.0
langgraph>=1.2.10

# Optional data sources.
google-auth>=2.35.0
google-auth-oauthlib>=1.2.0
google-api-python-client>=2.145.0

# Postgres. Only used in deployment, but installing it locally keeps the
# two environments honest.
psycopg[binary]>=3.2.0

# --- Website ---
streamlit>=1.60.0

# --- Tests ---
pytest>=8.0.0
httpx>=0.27.0
ruff>=0.6.0
